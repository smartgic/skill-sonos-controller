name: Localization validation

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  validate:
    uses: OpenVoiceOS/gh-automations/.github/workflows/locale-check.yml@dev
